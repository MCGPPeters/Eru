using System;


namespace Eru.Validation
{
    public static class ArgumentValidation
    {
        //public static Either<T, ArgumentNullException> IsNotNull<T>(this T t) =>
        //    t.Check(u => u != null)
        //        .Map(success => success)
        //        .MapMessages(msg => new ArgumentNullException(msg));

    }
}
